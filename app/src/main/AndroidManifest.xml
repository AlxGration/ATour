<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alex.atour">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ATour">
        <activity android:name=".ui.champ.ChampActivity"></activity>
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.ATour.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.registration.RegistrationActivity"
            android:label="@string/registration"
            android:theme="@style/Theme.ATour.NoActionBar" />
        <activity
            android:name=".ui.memrequest.MembershipRequestActivity"
            android:label="@string/request"
            android:theme="@style/Theme.ATour.NoActionBar" />
    </application>

</manifest>